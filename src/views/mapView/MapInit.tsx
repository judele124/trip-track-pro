import { ReactNode, useRef } from "react";
import useMapInit from "./hooks/useMapInit";

interface MapProps {
  routePoints?: { lat: number; lon: number }[];
  children?: ReactNode;
}

export default function MapInit({ children }: MapProps) {
  return (

  );
}
